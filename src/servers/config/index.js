/**
 * 配置模块导出
 */

const envConfig = require('./envConfig');
const mqConfig = require('./mqConfig');
const apiConfig = require('./apiConfig');

module.exports = {
  envConfig,
  mqConfig,
  apiConfig
};
